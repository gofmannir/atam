.global _start

.section .text

  mov $BitCheck, %rax
  cmpl $0, (%rax)
  jne bad_exit

  movq $60, %rax
  movq $0, %rdi
  syscall

bad_exit:
  movq $60, %rax
  movq $1, %rdi
  syscall

.section .data
Num1: .int 0
Num2: .int 1
BitCheck: .byte 0
